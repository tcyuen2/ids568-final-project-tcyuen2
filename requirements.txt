# ------------------------------------------------------------------
# Final Project pinned dependencies.
#
# Pinning rationale (per Module 0 / M2 reproducibility practices):
#   - sklearn / numpy: pinned to match the M3 training environment so
#     the bundled .pkl artifacts deserialize without warnings.
#   - prometheus-client: stable API, conservative pin to known-good.
#   - fastapi / uvicorn: latest 0.x stable as of project start; no
#     known incompatibilities with python 3.11 / 3.12.
# ------------------------------------------------------------------

# --- Serving stack ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2

# --- ML stack (must match M3 training env for pickle compatibility) ---
scikit-learn==1.3.2
numpy==1.26.2
pandas==2.1.4

# --- Monitoring (Component 1) ---
prometheus-client==0.20.0

# --- Drift detection (Component 4) ---
evidently==0.4.40
scipy==1.11.4

# --- A/B testing (Component 2) ---
statsmodels==0.14.2

# --- Plotting (Components 1, 4 outputs) ---
matplotlib==3.8.4

# --- HTTP client for traffic generator and tests ---
httpx==0.27.2
requests==2.32.3
